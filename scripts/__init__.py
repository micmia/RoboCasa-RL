"""Local training/eval scripts package.

This file exists so `import scripts.<module>` resolves to this repository's
`scripts/` directory rather than unrelated third-party packages named `scripts`.
"""

